ollama>=0.4.0
google-genai>=1.0.0
anthropic>=0.40.0
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
aiofiles>=23.0.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
chromadb>=0.5.0
# Arama araçları
duckduckgo-search>=6.0.0
requests>=2.31.0
python-multipart>=0.0.9
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
# Yeni ajan araçları
maigret>=0.6.0        # Sigma — OSINT
yfinance>=1.0.0       # Nova — Finans
pypdf>=3.0.0          # Multimodal PDF okuma
Pillow>=10.0.0        # Resim işleme
# Ziya — Müzik (Meta MusicGen via HuggingFace — Python 3.14 uyumlu)
# ace-step Python 3.14'te ÇALIŞMAZ: spaCy 3.8.4 için wheel yok
transformers>=4.40.0   # MusicGen + diğer HuggingFace modelleri
accelerate>=0.30.0     # GPU hızlandırma
scipy>=1.12.0          # WAV kaydetme
# PyTorch CUDA için: pip install torch --index-url https://download.pytorch.org/whl/cu128 --force-reinstall
playwright>=1.44.0    # Atlas — Web tarayıcı otomasyonu (pip install playwright && playwright install chromium)
playwright-stealth>=1.0.0  # Bot tespitini azalt
